# Superior

Webスクレイピングで画像URLを収集し、Excelファイルに保存するFlaskアプリ。

Part of the **Superior Series** — a set of tools for image management workflows.

## 起動方法

```bash
python -m venv venv
source venv/bin/activate
pip install flask pandas openpyxl requests selenium
python app.py
```
